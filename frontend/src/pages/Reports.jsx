import {useEffect,useState} from 'react';import {reportsApi} from '../lib/endpoints';export default function Reports(){const [data,setData]=useState(null);useEffect(()=>{reportsApi.dashboard().then(r=>setData(r.data))},[]);const csv=async()=>{const r=await reportsApi.exportCsv();const u=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=u;a.download='appointments.csv';a.click();URL.revokeObjectURL(u)};return <div><div className="flex justify-between mb-6"><div><h1 className="text-3xl font-bold">Reports</h1><p className="text-slate-500">Clinic performance and appointment reporting.</p></div><button onClick={csv} className="btn bg-slate-900 text-white">Export CSV</button></div><div className="card p-6 grid sm:grid-cols-2 lg:grid-cols-4 gap-5">{data&&Object.entries(data).map(([k,v])=><div key={k}><p className="text-sm text-slate-500 capitalize">{k.replaceAll('_',' ')}</p><p className="text-2xl font-bold mt-1">{String(v??'—')}</p></div>)}</div></div>}
