import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import {useAuth} from '../context/AuthContext'
export default function Register(){const {register}=useAuth();const nav=useNavigate();const [f,setF]=useState({full_name:'',email:'',password:'',role:'receptionist'});const [err,setErr]=useState('');const s=async e=>{e.preventDefault();try{await register(f);nav('/')}catch(x){setErr(x.response?.data?.detail||'Registration failed')}};return <div className="min-h-screen flex items-center justify-center p-6"><form onSubmit={s} className="card p-8 w-full max-w-md"><h2 className="text-2xl font-bold">Create account</h2>{err&&<p className="text-red-600 my-3">{err}</p>}{['full_name','email','password'].map(k=><input key={k} className="input mt-4" placeholder={k.replace('_',' ')} type={k==='password'?'password':k==='email'?'email':'text'} required value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<select className="input mt-4" value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="receptionist">Receptionist</option><option value="doctor">Doctor</option><option value="admin">Admin</option></select><button className="btn bg-blue-600 text-white w-full mt-5">Register</button><p className="text-center mt-4 text-sm"><Link className="text-blue-600" to="/login">Back to login</Link></p></form></div>}
