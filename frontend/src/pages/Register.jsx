import {useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import api,{apiError} from "../lib/api"; import {Stethoscope} from "lucide-react";
export default function Register(){const nav=useNavigate();const [error,setError]=useState("");const [busy,setBusy]=useState(false);const [form,setForm]=useState({full_name:"",email:"",password:"",role:"Receptionist"});const submit=async e=>{e.preventDefault();setError("");setBusy(true);try{await api.post("/auth/register",form);nav("/login");}catch(err){setError(apiError(err))}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-100 p-4"><div className="mx-auto flex min-h-[calc(100vh-2rem)] max-w-lg items-center"><div className="w-full rounded-3xl bg-white p-7 shadow-xl sm:p-10"><div className="mb-7 flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-brand-700 text-white"><Stethoscope size={18}/></span>CareFlow Clinic</div><h1 className="text-2xl font-bold">Create your account</h1><p className="mt-1 text-sm text-slate-500">Set up a staff account for the clinic workspace.</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<form onSubmit={submit} className="mt-6 space-y-4">{[["full_name","Full name","text"],["email","Email","email"],["password","Password","password"]].map(([k,l,t])=><div key={k}><label className="label">{l}</label><input required minLength={k==="password"?8:2} type={t} className="input" value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<div><label className="label">Role</label><select className="input" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>Receptionist</option><option>Doctor</option><option>Admin</option></select></div><button disabled={busy} className="btn-primary w-full">{busy?"Creating…":"Create account"}</button></form><p className="mt-5 text-center text-sm text-slate-500"><Link to="/login" className="font-semibold text-brand-700">Back to sign in</Link></p></div></div></div>}
