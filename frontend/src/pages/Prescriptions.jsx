import {useEffect,useState} from 'react';import api from '../lib/api';export default function Prescriptions(){const [items,setItems]=useState([]);useEffect(()=>{api.get('/prescriptions').then(r=>setItems(r.data)).catch(()=>{})},[]);return <div><h1 className="text-3xl font-bold">Prescriptions</h1><p className="text-slate-500 mt-1 mb-6">Previous prescriptions and treatment details.</p><div className="grid md:grid-cols-2 gap-4">{items.map(x=><div className="card p-5" key={x.id}><div className="flex justify-between"><b>Prescription #{x.id}</b><span className="text-sm text-slate-500">{String(x.created_at).slice(0,10)}</span></div><p className="mt-4"><b>Diagnosis:</b> {x.diagnosis}</p><p className="mt-2"><b>Medicines:</b> {x.medicines}</p><p className="mt-2"><b>Dosage:</b> {x.dosage}</p><p className="mt-2 text-slate-600">{x.instructions}</p></div>)}</div></div>}
