import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import {useAuth} from '../context/AuthContext'
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const submit=async e=>{e.preventDefault();try{await login(form);nav('/')}catch(x){setError(x.response?.data?.detail||'Login failed')}}
return <div className="min-h-screen grid lg:grid-cols-2"><div className="hidden lg:flex bg-slate-950 text-white p-16 flex-col justify-center"><p className="text-blue-400 font-semibold">MEDIFLOW+</p><h1 className="text-5xl font-bold mt-5">A calmer way to run your clinic.</h1><p className="text-slate-400 mt-5 max-w-lg">Appointments, patient records, prescriptions and operational reporting in one professional workspace.</p></div><div className="flex items-center justify-center p-6"><form onSubmit={submit} className="card p-8 w-full max-w-md"><h2 className="text-2xl font-bold">Sign in</h2><p className="text-slate-500 mt-1 mb-6">Access your clinic workspace.</p>{error&&<div className="bg-red-50 text-red-700 p-3 rounded-lg mb-4">{error}</div>}<label className="text-sm font-medium">Email<input className="input mt-1 mb-4" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="text-sm font-medium">Password<input className="input mt-1 mb-5" type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="btn bg-blue-600 text-white w-full">Sign in</button><p className="text-sm text-center mt-5">New user? <Link className="text-blue-600 font-semibold" to="/register">Create account</Link></p></form></div></div>}
